## Instrucciones

1. Clonar el repositorio.
2. Instalar dependencias con `npm install`.
3. Ejecutar `npm run dev` y abrir el enlace que entrega Vite (por defecto http://localhost:5173).
4. Para obtener una build optimizada ejecutar `npm run build` y, si deseas validarla localmente, `npm run preview`.
