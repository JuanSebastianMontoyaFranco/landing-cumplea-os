## Landing Cumpleaños

El código fuente vive ahora dentro de `client/`. Ingresa a esa carpeta para instalar dependencias (`npm install`), ejecutar el modo desarrollo (`npm run dev`) o generar builds (`npm run build`).
